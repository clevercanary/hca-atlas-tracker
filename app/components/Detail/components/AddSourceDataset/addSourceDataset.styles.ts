import styled from "@emotion/styled";
import { Dialog as TrackerDialog } from "../../../common/Dialog/dialog.styles";

export const Dialog = styled(TrackerDialog)`
  .MuiPaper-root {
    max-width: 712px;
    width: 100%;
  }
`;
