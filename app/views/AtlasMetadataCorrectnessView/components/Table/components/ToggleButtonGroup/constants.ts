import { Meta } from "../../hooks/useTable/entities";

export const OPTIONS: Record<keyof Meta, string> = {
  organSpecific: "Organ Specific",
  recommended: "Recommended",
  required: "Required",
};
